source 'https://rubygems.org'
# Pin this. Unconstrained, bundler will happily backtrack a decade to
# github-pages 8 (2013) if the current version can't resolve on your Ruby,
# then fail compiling C extensions that predate Ruby 2.4.
gem 'github-pages', '~> 232', group: :jekyll_plugins
